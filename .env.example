# Local PostgreSQL
# Example:
# DATABASE_URL=postgresql://localhost:5432/ekg_product
DATABASE_URL=postgresql://localhost:5432/ekg_product

# OpenAI (required for AI-powered features)
# OPENAI_API_KEY=your_openai_api_key

# Optional Replit AI Integrations
# AI_INTEGRATIONS_OPENAI_API_KEY=
# AI_INTEGRATIONS_OPENAI_BASE_URL=

# Embedded EKG engine (single-app mode)
EKG_ENGINE_MODE=embedded
EKG_ENGINE_AUTO_START=true
EKG_ENGINE_HOST=127.0.0.1
EKG_ENGINE_PORT=8787
# Deep mode model/runtime controls
# Default model keeps deep mode available without org verification blockers.
EKG_DEEP_MODEL=gpt-5.1
# Set true only if using background-capable model and async deep polling.
EKG_DEEP_BACKGROUND_MODE=false
# Optional if different from ekg_engine/.venv/bin/python
# EKG_ENGINE_PYTHON_BIN=/absolute/path/to/python

# EKG domain/vector store config (required for answer generation)
# DOC_VECTOR_STORE_ID=vs_xxx
# KG_VECTOR_STORE_ID=vs_xxx
# Helper: npm run vectorstores:list -- --doc-name "<doc>" --kg-name "<kg>" --write-env
# Optional domain-specific overrides
# PUDA_ACTS_REGULATIONS_VECTOR_STORE_ID=vs_xxx
# PRE_SALES_VECTOR_STORE_ID=vs_xxx
# APF_VECTOR_STORE_ID=vs_xxx

# KG path (required in all environments; use gs://... in production)
PUDA_ACTS_REGULATIONS_KG_PATH=gs://your-bucket/path/to/puda_master_kg.json
# Local development alternative:
# PUDA_ACTS_REGULATIONS_KG_PATH=/absolute/path/to/puda_master_kg.json
# PRE_SALES_KG_PATH=/absolute/path/to/presales_graph.json
# APF_KG_PATH=/absolute/path/to/apf_graph.json

# External EKG endpoint (only if EKG_ENGINE_MODE=external)
# EKG_API_URL=https://your-ekg-service

# Server runtime
NODE_ENV=development
PORT=5000
